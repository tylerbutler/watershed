*.beam
*.ez
/build
/node_modules
erl_crash.dump

# The reference relay's default data directory (`just relay`). A durable
# relay writes one append-only JSONL log per room; none of it belongs in git.
/relay-data

# Local tool state
/.superpowers/
/.claude/
.impeccable/
/.playwright-mcp/
/.serena/
/.worktrees/

# Netlify's offline preview server state and generated edge-runtime lockfile.
/.netlify/
/deno.lock

# APM dependencies
apm_modules/
