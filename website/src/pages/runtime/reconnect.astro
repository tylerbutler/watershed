---
import RuntimeLayout from "../../components/RuntimeLayout.astro";
---

<RuntimeLayout
  slug="reconnect"
  description="How a watershed client rejoins after a dropped connection. The client resubmits pending operations and loads an available summary or retained history."
>
  <div class="g-block">
    <h2>A dropped link is not a lost document</h2>
    <p>
      A laptop can sleep, a tunnel can close, or a phone can lose its signal.
      watershed reconnects the transport and assigns the new client ID. It then
      resubmits pending operations against the server’s current reference
      sequence number before it resumes live delivery.
    </p>
  </div>

  <div class="g-block">
    <h2>Rehydrate from a summary</h2>
    <p>
      A long-lived document can contain thousands of operations. Replaying all
      operations on each connection makes startup time increase with document
      age.
      An application can call <code>summarize(document)</code> to
      write the client’s current confirmed channel snapshots to floodgate storage.
      The server records the sequence number covered by that summary. On a later
      bootstrap, watershed loads an available summary and applies operations
      after that point.
    </p>
    <p>
      Each channel can serialize and rebuild its state. The snapshot contains
      confirmed state and the metadata required by the merge rule. An{" "}
      <a href="/structures/sets#orset">OR-set</a>'s add tags and a
      {" "}
      <a href="/structures/sets">2P-set</a>'s tombstones are in the summary.
      Thus, add-wins and permanent-removal rules remain after reload.
    </p>
  </div>

  <div class="g-block">
    <h2>Summary when available, history when not</h2>
    <p>
      A fresh bootstrap does not require a summary. If the server provides one,
      the runtime seeds channels from it and replays the supplied tail. Without
      one, it starts empty and replays the retained initial messages. If that
      batch begins after a missing prefix, the runtime pages the gap from the
      deltas endpoint before declaring the document ready.
    </p>
    <p>
      Kernel demos also exercise <code>from_summary</code> directly by building
      baseline replicas under a dedicated <code>survey-baseline</code>
      identity. That validates channel snapshot decoding. The transport
      reconnect path uses a different process. Because snapshots include
      required sequence metadata,
      order-sensitive structures like <a href="/structures/coordination#claims">claims</a>{" "}
      stay correct after a load. A slot that was claimed before the connection
      dropped is still claimed after reconnection. A compare-and-set still
      refers to the correct prior state.
    </p>
    <div class="g-note">
      <p>
        <strong>Reconnection synchronizes existing state.</strong> The client
        resubmits pending durable operations and receives missing confirmed
        history. A summary does not contain temporary signals. Thus, the client
        does not receive <a href="/runtime/presence">ripples</a> sent while it
        was disconnected. Store durable data in a channel.
      </p>
    </div>
  </div>

  <div class="g-out">
    <span class="annot">In short</span>
    <p>
      A summary is an optional checkpoint. Bootstrap uses an available summary
      or replays retained history. Reconnection preserves pending durable
      operations by resubmitting them in the resumed session.
    </p>
  </div>
</RuntimeLayout>
