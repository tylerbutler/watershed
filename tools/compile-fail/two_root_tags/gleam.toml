name = "two_root_tags"
version = "1.0.0"
description = "A fixture that MUST NOT compile — see src/two_root_tags.gleam"
licences = ["MIT"]
gleam = ">= 1.7.0"
target = "javascript"

[dependencies]
gleam_stdlib = ">= 0.62.0 and < 2.0.0"
gleam_json = ">= 3.0.0 and < 4.0.0"
gleam_javascript = ">= 1.0.0 and < 2.0.0"
watershed = { path = "../../.." }
