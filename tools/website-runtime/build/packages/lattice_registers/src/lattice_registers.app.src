{application, lattice_registers, [
    {vsn, "2.0.0"},
    {applications, [gleam_json,
                    gleam_stdlib,
                    lattice_core]},
    {description, "Last-writer-wins and multi-value registers for Gleam — conflict-free replicated register types"},
    {modules, [lattice_registers@@main,
               lattice_registers@lww_register,
               lattice_registers@mv_register]},
    {registered, []}
]}.
