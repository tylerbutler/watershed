{application, lustre, [
    {vsn, "5.7.1"},
    {applications, [exception,
                    gleam_erlang,
                    gleam_json,
                    gleam_otp,
                    gleam_stdlib,
                    houdini]},
    {description, "Create HTML templates, single page applications, Web Components, and real-time server components in Gleam!"},
    {modules, [lustre,
               lustre@@main,
               lustre@attribute,
               lustre@component,
               lustre@dev@query,
               lustre@dev@simulate,
               lustre@effect,
               lustre@element,
               lustre@element@html,
               lustre@element@keyed,
               lustre@element@mathml,
               lustre@element@svg,
               lustre@event,
               lustre@internals@constants,
               lustre@internals@json_object_builder,
               lustre@internals@mutable_map,
               lustre@internals@ref,
               lustre@runtime@app,
               lustre@runtime@server@runtime,
               lustre@runtime@transport,
               lustre@server_component,
               lustre@vdom@cache,
               lustre@vdom@diff,
               lustre@vdom@patch,
               lustre@vdom@path,
               lustre@vdom@vattr,
               lustre@vdom@vnode]},
    {registered, []}
]}.
